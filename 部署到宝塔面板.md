# 部署到宝塔面板指南

## 准备工作

1. 确保已在宝塔面板中安装Python环境（建议使用Python 3.8或更高版本）
2. 注册并配置好微信公众号，获取AppID、AppSecret和模板消息ID

## 部署步骤

### 1. 创建项目目录

在宝塔面板中创建一个新的网站或Python项目，例如：
```
/www/wwwroot/gold-price
```

### 2. 上传代码

将项目代码上传到服务器目录中，可以使用以下方式：
- 通过宝塔面板文件管理器上传
- 使用Git克隆项目
- 使用FTP工具上传

### 3. 安装依赖

在项目目录中打开终端，执行以下命令安装依赖：

1.更新系统,确保你的系统是最新的：
```bash
sudo yum update -y
```
2.安装必要的依赖 ,Playwright 需要一些编译工具和其他依赖项，如 gcc 和 glibc 等：
```bash
sudo yum groupinstall "Development Tools" -y
sudo yum install -y wget openssl-devel bzip2-devel libffi-devel zlib-devel xz-devel
sudo yum install -y atk-devel gcc-c++ gtk3-devel libsndfile-devel alsa-lib-devel
```
3.安装 Playwright ,在可以安装 Python Playwright：
卸载旧版本
```bash
pip3 uninstall playwright
```
安装Playwright
```bash
pip3 install playwright
pip install playwright
playwright install
```
4.如果您仍然遇到问题，请尝试更新您的gcc版本。在CentOS 7上，您可以使用以下命令安装更新的gcc：
```bash
sudo yum install centos-release-scl
sudo yum install devtoolset-8-gcc*
scl enable devtoolset-8 bash
```
安装依赖列表
```bash
pip install -r requirements.txt
```
解决文章中出现的错误
https://ask.csdn.net/questions/8153654/54894304
### 4. 安装Playwright浏览器（重要）

由于项目使用Playwright抓取网页数据，需要安装浏览器组件：

```bash
playwright install chromium
```

如果上述命令不可用，可以使用Python模块方式：

```bash
python -m playwright install chromium
```

如果还有问题，可以尝试安装所有依赖：

```bash
python -m playwright install-deps
```

### 5. 配置项目

编辑 `config.py` 文件，填入你的微信公众号配置信息：

```python
# 微信公众号配置文件

# AppID 和 AppSecret（从微信公众平台获取）
# 获取路径: 登录微信公众平台 -> 开发 -> 基本配置
APP_ID = 'your_actual_app_id'
APP_SECRET = 'your_actual_app_secret'

# 模板ID（在微信公众平台模板消息功能中设置）
# 获取路径: 登录微信公众平台 -> 功能 -> 模板消息
TEMPLATE_ID = 'your_template_id'

# URL（点击消息跳转的网址）
WEB_URL = 'https://www.yourwebsite.com/'

# 推送配置
PUSH_INTERVAL_MINUTES = 30  # 推送间隔（分钟）
MAX_PUSH_COUNT = 1  # 每次触发推送的最大次数
```

获取微信配置的详细步骤：

1. 登录微信公众平台：https://mp.weixin.qq.com/
2. 进入左侧菜单"开发" -> "基本配置"获取AppID和AppSecret
3. 进入左侧菜单"功能" -> "模板消息"创建模板并获取模板ID
4. 确保公众号已通过认证并开启了相关接口权限

### 6. 设置运行环境

在宝塔面板中添加Python项目：

1. 进入"软件商店"，确保已安装"Python项目管理器"
2. 进入"Python项目"，点击"添加项目"
3. 选择项目目录，设置项目名称
4. 选择Python版本
5. 启动方式选择"文件启动"，启动文件选择 `data.py`（持续监控模式）或 `main.py`（定期间隔推送模式）
6. 点击"提交"完成项目创建

### 7. 配置守护进程

为了确保项目在服务器重启后能自动运行：

1. 在Python项目管理器中找到你的项目
2. 点击"设置"，确保"开机自启"选项已开启
3. 可以设置日志文件路径以便排查问题

### 8. 配置定时任务（可选）

如果使用 `main.py` 模式，可以通过宝塔的"计划任务"功能来定期执行：

1. 进入"计划任务"
2. 选择任务类型为"Shell脚本"
3. 设置执行周期（如每30分钟执行一次）
4. 脚本内容示例：
```bash
cd /www/wwwroot/gold-price && python main.py
```

### 9. 查看运行状态和日志

1. 在Python项目管理器中可以查看项目运行状态
2. 查看日志文件以排查问题：
   - 默认日志路径通常在项目目录下
   - 或在Python项目设置中指定的日志路径

## 常见问题及解决方案

### 1. Playwright报错

如果遇到Playwright相关错误，尝试重新安装：
```bash
playwright install-deps
playwright install chromium
```

### 2. 网络连接问题

如果出现网络连接错误，如超时问题：

1. 检查服务器网络连接是否正常
2. 尝试在服务器上使用curl或ping测试网络连通性：
   ```bash
   ping finance.sina.com.cn
   ```
3. 必要时配置代理
4. 如果是服务器防火墙问题，需要在宝塔面板或服务器中开放相关端口

### 3. 微信接口调用失败

检查以下配置是否正确：
- AppID和AppSecret是否正确
- 公众号是否已认证
- 接口权限是否已开启
- 服务器IP是否已在公众号后台配置白名单

如果遇到 `invalid appid` 错误：
1. 确认在[config.py](file:///E:/Python/wechat/config.py)中填入了正确的AppID
2. 确认AppID是从微信公众平台获取的正确信息
3. 确认公众号已通过认证

### 4. 定时任务不执行

检查宝塔计划任务配置：
- 确保执行周期设置正确
- 检查脚本路径是否正确
- 查看任务执行日志排查问题

```bash
cd /www/wwwroot/python/wechat/gold
unset _BT_PROJECT_ENV && source /www/server/panel/script/btpyprojectenv.sh gold 
source /www/server/python_project/vhost/env/gold.env

```
### 定时重启python项目
```bash
btpython /www/server/panel/script/restart_project.py python gold

```
### 定时运行python项目
```bash
# 进入项目目录
cd /www/wwwroot/python/wechat/gold
# 清除旧的 Python 项目环境变量，并激活名为 gold 的宝塔 Python 项目环境
unset _BT_PROJECT_ENV && source /www/server/panel/script/btpyprojectenv.sh gold
# 进入宝塔python虚拟项目环境源
source /www/server/python_project/vhost/env/gold.env
# 执行
python main.py
```
### 定时运行指定版本python项目
```bash
# 进入项目目录
cd /www/wwwroot/python/wechat/gold
# 清除旧的 Python 项目环境变量，并激活名为 gold 的宝塔 Python 项目环境
unset _BT_PROJECT_ENV && source /www/wwwroot/python/wechat/gold/venv/bin/activate
# 执行指定版本
/www/server/pyporject_evn/versions/3.13.7/bin/python3.13 main.py
```
### 定时清除python项目
```bash
# 进入项目目录
cd /www/wwwroot/python/wechat/gold
# 清除旧的 Python 项目环境变量，并激活名为 gold 的宝塔 Python 项目环境
unset _BT_PROJECT_ENV && source /www/server/panel/script/btpyprojectenv.sh gold
# 进入宝塔python虚拟项目环境源
source /www/server/python_project/vhost/env/gold.env
# 执行
python clear_cache.py
```

## 监控和维护

1. 定期检查日志文件，确保程序正常运行
2. 关注微信接口调用次数限制
3. 根据实际需求调整推送频率和阈值
4. 定期更新依赖包以确保安全性